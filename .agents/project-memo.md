## NOVA foundation

- AUTH-001 functional session boundary: `apps/web/src/features/admin/admin-auth.ts` owns scoped staff/admin cache matching and 401-only session expiry handling; `apps/web/src/main.tsx` installs the shared QueryCache/MutationCache callbacks; `apps/web/src/app.tsx` owns the factor-aware memory-only staff form, permission-denied state and responsive logout controls. Admin mutations carry scoped keys so a staff 401 removes protected query and mutation state without clearing customer/cart data; 403 preserves the session and cache. Exact staff-login visual reference, viewport and required states remain required for final QA.
- `apps/api/src/dev.ts` is the API development process boundary: it builds compiled Nest output, watches the complete source tree including directories created after startup, debounces rebuilds, preserves the current server on build failure, restarts the compiled server after a successful build, and shuts down child/watcher resources on signals. Run it through `bun run dev:api`; direct Bun execution of Nest TypeScript source remains unsupported because decorator metadata is emitted by the compiled path.
- Verified 2026-09-11: AUTH-001 commit `03261b0` passed focused auth/main tests `9/9`, full workspace tests `442/442` across 95 files, root typecheck, lint, elevated production build, live API health/catalog/protected-route smoke and a new-source-directory watcher restart cycle against the healthy isolated PostgreSQL 16 runtime.

- Latest SEO/content slice: `apps/api/src/modules/content` owns `GET /v1/seo/resolve`, published-only `GET /v1/content/pages/:slug`, admin-only paginated SEO metadata/redirect CRUD, and admin content-page list/detail/create/update/status mutations. Content pages are draft-first, use the normalized Latin slug policy, validate bounded ordered JSON blocks, allow only explicit lifecycle transitions, require content before publishing, and audit mutations transactionally with optional `updatedAt` optimistic concurrency. Public paths/canonical URLs are constrained to the configured site origin, redirects reject external destinations and bounded cycles, and migration `0006_seo_redirect_status` constrains persisted redirect status codes to `301`, `302`, `307`, or `308`. `apps/web/src/features/content/content-api.ts` exposes page-agnostic transport/query/mutation helpers; the decisions are [`docs/adr/0020-seo-metadata-and-redirects.md`](../docs/adr/0020-seo-metadata-and-redirects.md), [`docs/adr/0021-public-content-page-read.md`](../docs/adr/0021-public-content-page-read.md), and [`docs/adr/0022-admin-content-page-lifecycle.md`](../docs/adr/0022-admin-content-page-lifecycle.md). Visual SEO/content pages remain gated on supplied references, and live PostgreSQL execution remains unverified.

- Verified 2026-09-11: the SEO-001/CONTENT-001 integration is local at `codex/integration` merge `27116b72` from SEO task commit `770c96e`. `apps/web/src/ssr/server.ts` consumes the published-only `/v1/content/pages` summary index, validates content/catalog/pagination source shapes, fails closed with `503`/`no-store`, bounds resolver work to 16 concurrent requests with abort-on-failure, and enforces the standard 50,000 URL/50 MiB sitemap limits. `packages/api-client/src/content-contract.ts` is the shared public-slug policy source. Both independent review axes accepted the final follow-up; focused tests passed `50/50`, web/api-client/api typechecks, lint/format, diff-check and the elevated web client+SSR build passed. Live API/database/browser/crawler execution remains a batched, unrun gate; remote push/PR creation remains blocked by repository-export access.
- Verified 2026-09-11: SSR deadline follow-up `e6ca5e6` in `apps/web/src/ssr/server.ts` applies one configurable five-second default deadline to public document and sitemap API reads, propagates a shared abort signal through resolver/catalog/content calls, and returns fail-closed `503`/`no-store` responses on expiry. The focused SSR suite passes `27/27`, web typecheck and Prettier pass, the approved local client+SSR build passes, and live latency/upstream cancellation remains unverified.
- Verified 2026-09-11: SEO hydration follow-up `5c468df` serializes public home/category/product/content data from `apps/web/src/ssr/server.ts` into the initial context and `apps/web/src/main.tsx` seeds the matching TanStack Query keys before the interactive app mounts. Category SSR uses the client-aligned `limit=4`; focused main/metadata/SSR tests pass `35/35`, web typecheck, Prettier, diff-check and the approved client+SSR build pass. Live browser/network duplicate-request tracing remains unverified.
- Verified 2026-09-11: ADMIN-002 task commit `e11e087` and ADMIN-003 task commit `3b9f58d` were independently scoped to page/test files and locally integrated; parent commit `2f533f2` wires admin order list/detail plus payment, customer, notification and audit routes in `apps/web/src/app.tsx`. The supplied `atelier-admin-operations.png` reference was adopted. Focused page/API tests pass `12/12`, web typecheck, targeted ESLint/Prettier, diff-check and the client+SSR build pass. Desktop rendered inspection passed through the available browser harness; live authenticated operations/inspection, mobile viewport override and Chromium pixel comparison remain unverified. ADMIN-002's requested integration-base PR is blocked by the remote base branch; ADMIN-003 task PR #5 remains open into `master`.

- Latest catalog search slice: `GET /v1/search/suggestions` returns a bounded, typed category/product list from published catalog data. It reuses Persian normalization, matches product search text/title/slug/active SKU plus category name/slug, includes only a primary product image, and returns no database read for a blank query. `apps/web/src/features/catalog/catalog-api.ts` exposes the versioned path, request builder, fetcher, and `useCatalogSuggestions`; the durable contract is [`docs/adr/0019-catalog-search-suggestions.md`](../docs/adr/0019-catalog-search-suggestions.md).
- Latest catalog search slice validation: public catalog and browser transport tests pass 13/13; workspace typecheck, lint, and production build also pass. PostgreSQL execution of the new SQL ranking query remains unverified because local PostgreSQL is unavailable.

- Latest catalog facets slice: `GET /v1/catalog/facets` and `useCatalogFacets` expose bounded, contextual size/color/material groups. The API retains route/query context, counts distinct published products while excluding each group’s own filter, reads only active variant/product-attribute values, preserves selected stale values, and returns color hex metadata without inventory quantities. The accepted contract is [`docs/adr/0023-catalog-facets.md`](../docs/adr/0023-catalog-facets.md); PostgreSQL execution of the raw facet query remains unverified because local PostgreSQL is unavailable.
- Live catalog-facet smoke: an isolated, non-persistent cached PostgreSQL 18.4 container was migrated via the six existing SQL files, seeded with 9 categories and 6 products, and exercised through the API. `/health/ready` reported `database: ok`; unfiltered facets returned real product counts, contextual `audience/size/inStock` facets narrowed correctly, stale `xxl`/`مشکی` selections were retained at count 0, and `onSale=true` returned one seeded product. The repository’s exact PostgreSQL 16 image could not be pulled because Docker Hub DNS/proxy access failed; Prisma 7.10’s Windows schema engine also returned an empty error before migration SQL was sent, so `prisma migrate deploy` remains a separate validation gap.

- The storefront search dialog now consumes the bounded `useCatalogSuggestions` query with a 220ms debounce. Product suggestions link to the product detail route and category suggestions link to the filtered catalog route; blank search stays local and does not query the product list. Product admin writes reject compare-at prices that are not greater than the active price, and the storefront mapper hides malformed legacy compare-at values instead of presenting a false sale.
- Effective catalog pricing: variant compare-at values inherit the product compare-at only when they remain above the effective variant price; public catalog, cart, checkout, and storefront mapping sanitize malformed legacy values, admin product/variant price writes reject changes that would invalidate the invariant, and the sale filter includes valid active variant-level sales.
- Customer session/cart handoff: `apps/web/src/app.tsx` observes the customer session and automatically calls the existing transactional guest-cart merge only for a cached guest cart, routing structured merge conflicts to the existing conflict state; customer verification discards only a previously cached customer cart when switching identities, and logout removes the customer cart query so a later guest session cannot display stale customer-owned contents. The ownership guards are directly covered by browser regression tests.

- Previous catalog transport slice: `apps/web/src/features/catalog/catalog-api.ts` exposes the authoritative `GET /v1/catalog/categories` adapter and `useCatalogCategories` query alongside product/search/detail reads; focused catalog/API tests passed 7/7.

- Guest-cart merge contract: `POST /v1/cart/merge` preflights lifecycle, stock, and quantity state inside the merge transaction. Any stale line returns a bounded `CART_MERGE_CONFLICT` `409` with per-variant quantities and leaves both carts unchanged; successful merges remain transactional. The browser adapter is `apps/web/src/features/cart/cart-api.ts`, and the durable decision is [`docs/adr/0018-cart-merge-conflict-contract.md`](../docs/adr/0018-cart-merge-conflict-contract.md).
- Latest cart-merge slice validation: the API error-filter, customer-cart merge, and browser transport tests pass 7/7; workspace typecheck, lint, and build also pass. The live database-backed merge path remains unexercised because local PostgreSQL is unavailable.

- Catalog admin write boundary: `POST /v1/admin/catalog/products` creates draft-first products and `PATCH /v1/admin/catalog/products/:productId` edits immutable-slug records with `updatedAt` optimistic concurrency; both assert the admin role in the use case and write audit events transactionally.
- Admin catalog list contract: `GET /v1/admin/catalog/products` now returns base/compare prices, active category summaries, primary media, and aggregate inventory status. Its normalized query supports lifecycle status, active category slug, and an exact low-stock product filter before pagination; the browser adapter exposes those filters to `AdminProductsPage`.

- `arch.md` is the current architecture source of truth. The repository has the Phase 1 foundation plus initial catalog/discovery, guest-cart, and checkout-time inventory-reservation vertical slices: a Bun workspace with `apps/web`, `apps/api`, `apps/worker`, and shared packages under `packages/`.
- `apps/web/src/app.tsx` is the current RTL foundation screen. It uses `@nova/api-client` through TanStack Query and reports API liveness with a retry action.
- `#admin/products` renders `AdminProductsPage` in `apps/web/src/app.tsx`, a Tailwind responsive operations view grounded in `apps/web/public/design-references/atelier-admin-operations.png`: right-side oxblood desktop rail, product table, search/category/status filters, low-stock and order queues, mobile cards, and admin bottom navigation. When a staff session is present it consumes the real admin catalog, category, inventory, and order hooks with server-side pagination, lifecycle/category/low-stock filters, product media/pricing/category summaries, and stock/order status mapping; development without a staff session keeps the explicitly labeled local NOVA preview. The products visual QA report is in [`design-qa.md`](../design-qa.md) and remains blocked only because the selected CUA browser could not persist or compose the required normalized comparison capture.
- `apps/web/src/features/catalog/catalog-api.ts` owns catalog transport adapters and TanStack Query hooks, including category/product reads and the bounded category/product search-suggestion query. Home, category, product listing, search, and product detail use the read-only catalog API. `apps/web/src/features/cart/cart-api.ts` owns the guest-cart query/mutation hooks. `apps/web/src/features/admin/admin-catalog-api.ts` owns staff-session transport plus admin catalog category/product/assignment/option/variant/media queries and mutations with cache invalidation. `apps/web/src/features/admin/admin-inventory-api.ts` owns page-agnostic inventory list/detail/adjustment/reorder-point transport and query/mutation hooks; the non-dashboard admin screens still await their supplied page references.
- Storefront cart recommendations in `CartPage` also use `useCatalogProducts`; the remaining hardcoded `products` collection is limited to preview/editorial/admin-legacy content until those screens receive their page-specific references or data contracts.
- `#account/addresses` now consumes the customer address hooks for authenticated list/create/edit/default/delete behavior. Edit links carry the address ID, the form includes every server-required field, and unauthenticated or failed reads render an explicit recovery state instead of submitting preview data.
- `#auth` now wires the existing OTP request/verify screens to `apps/web/src/features/auth/auth-api.ts`, carries only the phone number in session storage, routes the server challenge ID through the hash, and exposes provider/API failures without storing or displaying an OTP. `#account` consumes the current customer session and customer order summaries, and `#order/:orderNumber` consumes customer-scoped detail/timeline/address data. The account nav can log out through the session mutation; delivered order details link to the live return-request flow, which validates order items and required condition attestations before calling the server.
- `#checkout/address`, `#checkout/shipping`, and `#checkout/payment` now use customer addresses, preserve address/shipping/coupon state in the hash, fetch the authoritative quote, and submit with a stable idempotency key. A successful local payment handoff routes to a query-backed confirmation page or the provider redirect; the local payment adapter still fails closed until configured.
- `apps/api/src/main.ts` owns Nest bootstrap, CORS, the `/v1` prefix, validation, request IDs, and the shared error envelope. Health endpoints stay outside `/v1` at `/health/live` and `/health/ready`.
- `apps/api/src/modules/catalog` owns category, product, search, filter, sort, pagination, and the protected admin product read/write/lifecycle/assets/taxonomy boundary. Public reads return safe variant availability/color metadata without inventory quantities or reservations; `GET /v1/admin/catalog/products` allows seeded staff roles to inspect status/count metadata, while product create/edit is admin-only draft-first work with server-side normalization, immutable slugs, optimistic `updatedAt` guards, and transactional audit events. Product lifecycle mutations validate active-variant, primary-media, and complete-option-selection prerequisites. Variant/media list/read is available to staff read roles; writes are admin-only, variant creation creates a zero-stock inventory row, variants deactivate instead of delete, and published products cannot lose their last `PRODUCT` image. Category administration rejects parent cycles and archived parents, blocks archiving categories assigned to published products, and replaces product assignments only with active categories. Product option/value administration is admin-only for writes; keys remain stable, values expose variant-reference counts, and variant option-value associations are constrained to the same product with one value per option. The non-dashboard admin UI remains later work until its page references are supplied. Persian normalization maps Arabic/Persian letter and digit variants, punctuation, case, zero-width characters, and half-spaces without changing display text. Catalog products expose normalized `ProductOption`/`ProductOptionValue` data, variant option-value IDs, and ordered `VariantMedia`; the PDP consumes those IDs for compatible multi-option selection, uses variant media when available, and keeps a flat-field fallback for legacy data. The accepted lifecycle decision is recorded in [`docs/adr/0008-catalog-lifecycle-and-audit.md`](../docs/adr/0008-catalog-lifecycle-and-audit.md).
- `apps/api/src/modules/health` owns liveness and PostgreSQL readiness checks; `apps/api/src/database` owns the Nest database lifecycle boundary.
- `apps/api/src/modules/cart` owns the cookie-backed guest-cart API and authenticated customer carts. It stores only a hash of the opaque 30-day guest token, derives live prices and availability from the catalog/inventory rows, enforces server-side quantity limits, supports idempotent add requests through `CartMutation` receipts, serializes per-user customer-cart creation with a transaction advisory lock, and merges guest contents into the customer cart. Customer ownership is session-derived rather than client-controlled.
- `apps/api/src/modules/auth` owns the customer OTP/session foundation. OTP challenges are stored in Redis as HMAC verifiers with five-minute expiry, single-use consumption, attempt limits, phone/IP cooldowns, and latest-challenge invalidation; durable customer sessions store only opaque-token hashes in PostgreSQL with idle/absolute expiry and revocation. `UnconfiguredOtpDelivery` intentionally fails closed until an SMS provider is configured, so OTP delivery is not end-to-end operational yet.
- `apps/api/src/modules/staff-auth` owns the staff password + TOTP boundary. Passwords use scrypt, TOTP secrets are AES-256-GCM encrypted with `STAFF_TOTP_ENCRYPTION_KEY`, recovery codes are keyed digests with one-time conditional claims, Redis throttles account/IP failures, and staff sessions use `SessionKind.ADMIN` with separate idle/absolute limits. Roles are the seeded `support`, `operations`, and `admin` keys; public provisioning and broader admin mutations are not implemented yet. The accepted decision is recorded in [`docs/adr/0007-staff-authentication-and-sessions.md`](../docs/adr/0007-staff-authentication-and-sessions.md).
- `apps/api/src/common/http/csrf.guard.ts` applies exact configured-origin validation and double-submit CSRF tokens to state-changing requests. Safe requests issue the readable `nova_csrf` cookie; `packages/api-client` mirrors it into mutation headers, and cart/session cookie setters append rather than overwrite `Set-Cookie` values.
- `apps/api/src/modules/inventory` owns checkout-time stock reservations and the protected staff inventory boundary. `InventoryService.reserve` validates active published variants, sorts unique lines to reduce lock contention, atomically increments `InventoryItem.reserved` with a parameterized PostgreSQL conditional update, and creates one reservation/movement per line transactionally. `consume`, `release`, `expire`, and `expireDue` use conditional state claims so terminal transitions are idempotent and cannot double-adjust stock. `InventoryAdminService` exposes support/operations/admin reads plus operations/admin on-hand and reorder-point mutations; adjustments use current quantity/timestamp conditions, reject reductions below reserved stock, create signed `ADJUSTMENT` movements, and write the audit event in the same transaction. The accepted concurrency decision is recorded in [`docs/adr/0005-inventory-reservation-concurrency.md`](../docs/adr/0005-inventory-reservation-concurrency.md).
- `apps/api/src/modules/checkout` owns the first authoritative quote/order boundary. Quotes re-read live customer-cart prices and availability, resolve an owned address, and apply the replaceable local shipping policy (standard free, express 89,000 toman). Submit uses a unique idempotency key, snapshots order lines/address/totals, reserves inventory outside the order-intent transaction, persists a payment attempt and a default local `PENDING` shipment, and calls the payment adapter only after durable records exist. Failed payment handoff releases reservations and cancels the pending order. The local payment adapter intentionally fails closed until a provider is configured. The transaction boundary is recorded in [`docs/adr/0006-checkout-transaction-boundaries.md`](../docs/adr/0006-checkout-transaction-boundaries.md).
- `apps/api/src/modules/payments` owns provider-agnostic callback verification and reconciliation. It validates event identity, payload hash, order number, provider transaction, and amount; makes webhook replays harmless; consumes or reacquires checkout reservations; and records late-payment refunds with observable success/failure state. Provider integration remains fail-closed behind `PaymentGateway` until a real adapter is selected. The callback/reconciliation decision is recorded in [`docs/adr/0009-payment-callbacks-and-reconciliation.md`](../docs/adr/0009-payment-callbacks-and-reconciliation.md).
- `apps/api/src/modules/orders` owns customer-scoped order history/detail reads and staff order search/detail plus operations/admin-only fulfillment, shipment, cancellation, and return-review boundaries. Customer responses use immutable order line/address snapshots, return/refund progress, and omit provider transaction data and internal event reasons; staff support/operations/admin reads add customer identity and shipment summary fields while retaining the same safe event boundary. Fulfillment is paid-order-only, forward-only (`CONFIRMED` → `PREPARING` → `SHIPPED` → `DELIVERED`), requires a reason and optimistic `updatedAt`, and records order events/audit entries transactionally. Shipment state is forward-only (`PENDING` → `PACKED` → `SHIPPED` → `DELIVERED`), synchronizes the order status, and records tracking/timestamps. Customers can cancel unpaid orders with reservation release or paid `CONFIRMED` orders with an idempotent full-refund workflow; delivered paid orders can request full-line returns within seven days with condition attestations, and support/admin can approve, reject, receive, and retry the refund. Item-only refunds preserve `PAID` when shipping remains non-refunded; full returns can move the order/shipment to `RETURNED`. `apps/web/src/features/orders/orders-api.ts` and `apps/web/src/features/admin/admin-orders-api.ts` expose page-agnostic TanStack Query transport hooks; storefront account/order/return screens use the customer hooks, while `apps/web/src/features/admin/admin-orders-page.tsx` now owns the real admin order list/detail and operations UI and is route-integrated through `apps/web/src/app.tsx`. The fulfillment state boundary is recorded in [`docs/adr/0010-fulfillment-and-shipment-state.md`](../docs/adr/0010-fulfillment-and-shipment-state.md), and cancellation/returns in [`docs/adr/0011-cancellation-and-returns.md`](../docs/adr/0011-cancellation-and-returns.md).
- `apps/api/src/modules/audit` owns transactional audit writes plus the admin-only read boundary at `GET /v1/admin/audit-events`. `AuditAdminService` enforces the admin role in the use case, applies bounded exact filters and stable newest-first pagination, and returns recorded metadata without reconstructing payment secrets or raw provider payloads. `apps/web/src/features/admin/admin-audit-api.ts` exposes page-agnostic query transport; the route-integrated audit inspection UI lives in `apps/web/src/features/admin/admin-support-finance-page.tsx`. The read decision is recorded in [`docs/adr/0012-admin-audit-read.md`](../docs/adr/0012-admin-audit-read.md).
- `apps/api/src/modules/payments` also owns the admin-only payment inspection boundary at `GET /v1/admin/payments` and `GET /v1/admin/payments/:paymentAttemptId`. `PaymentAdminService` enforces the admin role in the use case, applies bounded exact status/provider/order filters, and returns attempt/order/refund state without redirect URLs, idempotency keys, raw gateway payloads, or webhook hashes. `PaymentService` is exported from `PaymentsModule` because order cancellation and return flows inject it. `apps/web/src/features/admin/admin-payments-api.ts` exposes page-agnostic query transport; the route-integrated payment inspection UI lives in `apps/web/src/features/admin/admin-support-finance-page.tsx`. The read decision is recorded in [`docs/adr/0013-admin-payment-inspection.md`](../docs/adr/0013-admin-payment-inspection.md).
- `apps/api/src/modules/notifications` owns transactional notification intents for payment success/failure events. `NotificationService` writes a unique dedupe key and bounded payload through the callback transaction, while `apps/worker/src/notification-worker.ts` claims pending or expired jobs conditionally, retries with capped exponential backoff, and records only a stable failure code. `NotificationAdminService` exposes operations/admin-only redacted delivery-state reads without recipient, payload, dedupe-key, or provider fields. `UnconfiguredNotificationSender` intentionally fails closed until a real provider is selected. The outbox decision is recorded in [`docs/adr/0014-notification-outbox.md`](../docs/adr/0014-notification-outbox.md), and the inspection boundary in [`docs/adr/0017-notification-inspection.md`](../docs/adr/0017-notification-inspection.md); the route-integrated admin notification inspection UI is in `apps/web/src/features/admin/admin-support-finance-page.tsx`, while customer notification UI and provider delivery remain later work.
- `apps/api/src/modules/coupons` owns bounded V1 fixed/percentage coupon preview, minimum-order and active-window validation, row-locked order-time reservations, idempotent commit/release transitions, and admin list/create/update operations. Coupon codes remain immutable, admin mutations are audited and guarded by `updatedAt` optimistic concurrency, committed redemptions remain consumed after refunds, and expired reservations are excluded from capacity and released during a later reservation check. `apps/web/src/features/admin/admin-coupons-api.ts` exposes page-agnostic admin transport; the coupon UI remains pending a supplied page reference. The lifecycle decision is recorded in [`docs/adr/0015-coupon-redemption-lifecycle.md`](../docs/adr/0015-coupon-redemption-lifecycle.md).
- `apps/api/src/modules/customers` owns the read-only `GET /v1/admin/customers` support lookup. Support, operations, and admin can search bounded phone/email/identifier terms and status, while the database predicate excludes users with staff credentials or roles. Results contain only customer identity/status, timestamps, order count, and latest-order context; `apps/web/src/features/admin/admin-customers-api.ts` exposes page-agnostic transport and the route-integrated lookup UI is in `apps/web/src/features/admin/admin-support-finance-page.tsx`. The privacy boundary is recorded in [`docs/adr/0016-customer-support-lookup.md`](../docs/adr/0016-customer-support-lookup.md).
- `packages/api-client` owns transport-safe health/error/auth/address/catalog/cart/checkout/admin-catalog/admin-inventory types, JSON mutation helpers including PUT, and query-key conventions. `packages/ui` owns shared tokens and primitives. `apps/worker/src/main.ts` remains the process boundary, and notification job processing lives in `apps/worker/src/notification-worker.ts` until a real provider and scheduler are configured.
- Local setup uses `bun install`, `bun run db:generate`, `bun run docker:config`, `bun run dev`, `bun run dev:api`, and `bun run dev:worker`.
- Confirmed locally: Prisma schema validation, client generation, and migration/schema diff with no drift; ESLint; full workspace build; API, worker, and database typechecks; 435 source and compiled tests across 94 files, including the SEO, published-content, admin content-page lifecycle, catalog pricing-invariant, contextual catalog-facet, and session/cart ownership-isolation tests; focused payment callback, notification outbox, notification inspection, coupon, customer lookup, cancellation, return, refund, order, fulfillment, audit, catalog, inventory, cart, address, checkout, staff-auth, CSRF, customer-auth, customer-address, customer-checkout, content, and browser transport tests; and Nest route registration for checkout, payment callback, staff auth, protected admin catalog category/product/assignment/option/variant/media routes, protected admin inventory routes, customer/admin order reads, customer cancellation/return actions, admin fulfillment/shipment mutations, admin return review, admin audit reads, admin payment reads, admin coupon reads/writes, admin customer lookup, admin notification inspection, and content/SEO routes. The most recent complete workspace suite evidence passes 435/435; the post-merge deterministic integration harness separately passes 8 suites and 131 underlying tests. A live API smoke check returned `200` for `/health/live` and `401` for the protected admin coupon, customer, notification, payment, and order reads; the temporary server was stopped. The worker was not run against a live queue/database because the batched live runtime gate remains pending.
- Prisma 7 uses `packages/db/prisma.config.ts` as the source of truth for the migration datasource URL and `migrations.seed`, `packages/db/src/generated/prisma` for the explicit `prisma-client` output, and `@prisma/adapter-pg` in `DatabaseClient` and the seed script. The `packages/db/package.json` legacy `prisma.seed` key was removed so `prisma db seed` uses the config-owned `bun prisma/seed.ts` command. Keep the CLI, client, and adapter on the same Prisma version.
- `packages/db/prisma/migrations/0001_foundation/migration.sql` is the initial schema migration. It creates `pg_trgm`, a normalized product search column, full-text/trigram GIN indexes, normalized product-option tables, `CartMutation` receipts, and non-negative inventory/positive reservation quantity checks. It has not been applied locally because PostgreSQL is not running on `127.0.0.1:5432`.
- `packages/db/prisma/migrations/0004_notification_outbox_dedupe/migration.sql` backfills a unique `NotificationJob.dedupeKey` for existing rows and makes payment notification producers idempotent across callback and transaction retries.
- `packages/db/prisma/migrations/0005_coupon_redemption_lifecycle/migration.sql` adds coupon minimum-order/enabled metadata and the durable reserved/committed/released redemption lifecycle used by checkout and payment reconciliation.
- The local API boots and registers catalog, checkout, staff-auth, protected admin catalog category/product/assignment/option/variant/media, admin payment, admin coupon, notification-dependent payment, and content/SEO routes without connecting to PostgreSQL at startup; liveness and protected-route smoke checks pass. Database-backed catalog/cart/address/checkout/staff-auth/catalog-admin/coupon/content/SEO reads and mutations remain unavailable until PostgreSQL is running on `127.0.0.1:5432`; OTP delivery, staff login throttling, checkout payment, and notification delivery require their configured Redis/SMS/payment/notification providers.
- Verified 2026-09-11: ADMIN-001 (`0daabff`) and ADMIN-004 (`e525ac8`) are locally integrated through `a0146b7`/`ede0745` plus parent route/fix commit `ba864c6`. Catalog/categories/products/inventory and content/pages/SEO/redirects are now reachable from `apps/web/src/app.tsx`; combined focused page tests pass `14/14`, web typecheck, targeted ESLint/Prettier, `git diff --check` and the approved client+SSR build pass. The supplied `atelier-admin-operations.png` reference was adopted. Live authenticated operations/mutations, mobile viewport comparison and Chromium pixel capture remain unverified. ADMIN-001 direct product-by-ID loading is bounded by the existing list-only admin product contract because no detail GET endpoint exists; no endpoint was invented. Local HEAD is clean `ba864c6` on `codex/integration`, ahead of `origin/master` by 10 commits; parent push/PR synchronization remains blocked.

## Live execution ledger

- Verified 2026-09-10: OPS-002's one-file CI prerequisite fix is already included in local `codex/integration` through merge `8e1cda7` from task commit `32d105a`; local Prisma generation and root typecheck passed, while remote PR/push remains blocked.

- The current operational status and agent/PR update rules live in [`docs/remaining-work-status.md`](../docs/remaining-work-status.md). Update it after every merge, dispatch, resume/stop, PR state change, validation result or blocker; do not infer completion from an agent report alone.
- Verified 2026-09-10 (superseding the earlier integration snapshot): local `codex/integration` is `808dd7634a357eb45eb61da4e9b320c7146f2d62`, combining the accepted API-001, TEST-001, OPS-002, WEB-001 and OPS-001 task revisions. API-001 remains locally review-ready while PR #2 points remotely at `17c0de0`; TEST-001 remains in open PR #4; WEB-001 and OPS-001 have no remote PR because repository export/push is blocked.
- Verified 2026-09-10: local integration baseline `codex/integration` is `dc54fee66f2100414e23c20077f532f8ea36e239`, combining API-001 (`1f0237a`), TEST-001 (`86355d3`), OPS-002 (`32d105a`) and the accepted WEB-001 extraction (`e998c08`). API-001 is locally review-ready but PR #2 still points remotely at `17c0de0` because GitHub DNS blocked the push. WEB-001 has no remote PR because repository export/push access is blocked; TEST-001 remains in open PR #4.
- Verified 2026-09-10: AUTH-001 preflight found the staff login/me/logout API, opaque HttpOnly session cookie, CSRF, roles and browser transport/hooks already present. The browser login form, central admin guard, logout/cache invalidation, expiry/403 handling and focused session tests remain to be implemented. Exact staff-login reference, viewport and required states are required before changing login UI composition.
- Historical pre-update check on 2026-09-10: the unrelated `docker-postgres-1` container used `postgres:latest`, was stopped with exit code `255`, and did not provide the repository's required `postgres:16-alpine` runtime.
- Historical pre-update pull failure on 2026-09-10: Docker Desktop initially could not resolve/reach `auth.docker.io` through its HTTPS proxy; the unrelated stack was not modified.
- Verified 2026-09-10: isolated Compose project `nova-pg-check-20260910` now runs healthy `postgres:16-alpine` on port `55432`. `pg_isready`, PostgreSQL `16.15`, all six migrations, idempotent seed, and read-only counts (9 categories, 6 products, 14 variants, 6 media, 14 inventory) passed.
- Verified 2026-09-10: OPS-001 worker observability was accepted from Aristotle's `9e69d54` on `codex/ops-001-worker-observability` and merged locally into `codex/integration` as `808dd76` with an `apps/worker`-only write boundary. Focused worker tests `10/10`, typecheck, build, lint, formatting and diff-check passed; batch live checks at wave/integration gates instead of after every task. No remote PR exists because repository export/push is blocked.
- CI currently fails at root `Typecheck` on master and PRs. Local inspection found the clean workflow omitted Prisma generation; the bounded fix is committed locally as `32d105a` on `codex/ops-002-ci-recovery`, but push/create-PR is blocked by `github.com` DNS resolution. Remote compiler diagnostics remain unavailable.
- Verified 2026-09-11: the bounded OPS-002 PostgreSQL archive/restore verifier from `codex/ops-002-backup-restore` task head `05b5c89` is merged locally into `codex/integration` as `c4f2aa6`. It keeps database URLs in process environment, scrubs ambient libpq settings, pins resolved host addresses, passes only validated simple database names to clients, uses `--no-password`, compares `pg_control_system()` cluster identities around dump/restore, writes archives through unique partial paths and non-overwriting publication, and requires explicit exclusive target-maintenance approval. The runbook deliberately limits this to provider-independent evidence; live restore, external destination, encryption/key custody, retention/WAL/media/monitoring/rollback decisions remain open.
- Verified 2026-09-11: the isolated `nova-pg-check-20260910-postgres-1` runtime is currently healthy from `postgres:16-alpine` on host port `55432`. Read-only checks accepted `pg_isready`, reported PostgreSQL `16.15`, found six finished Prisma migrations, and returned the seeded counts of 9 categories, 6 products, 14 variants, 6 media records and 14 inventory records. This is runtime/migration/seed evidence only; live application flows, worker execution and backup/restore remain separate gates.

## Verified 2026-09-11 storefront integration and next gates

- `apps/web/src/app.tsx` is the parent route-composition owner. WEB-002 discovery/cart (`36efd32`, `47cb203`), WEB-003 account/orders (`5df3fb8`), WEB-004 checkout/recovery (`b7e18b4`) and WEB-005 content/system (`e4b756b`) are route-integrated on `codex/integration` by parent commit `465ce07`; the page-local modules remain the behavior owners and shared route wiring stays parent-owned.
- The integrated storefront baseline passed the web typecheck and a focused 27-test route/page/state suite; the parent route diff passed `git diff --check`. Live authenticated/browser/mobile pixel evidence remains open, and provider adapters remain blocked on provider/sandbox/input decisions.
- AUTH-001 now has the project-bound design target `output/design-artifacts/auth-staff-login-atelier.png` for `1440x900` and `390x844`. It is a visual reference only, not product or runtime truth; same-viewport route/state/accessibility evidence is still required.
- Verified 2026-09-11: AUTH-001 visual follow-up `03ed76f` owns the staff-login composition in `apps/web/src/app.tsx` and adds only the required `mail`/`shield` primitives in `apps/web/src/shared/icon.tsx`. The available CUA runtime shows the default-viewport `#admin/login` composition aligned with `output/design-artifacts/auth-staff-login-atelier.png`; typecheck, the 132-test web/e2e suite, targeted Prettier, diff-check and the elevated client+SSR build pass. Exact target-size (`1440x900`/`390x844`) state, AT and pixel evidence remain open, and no credentials or external 12ui transmission were used.
- QA-001 James (`01a09128-5e2b-7921-84e6-47f28a1844e8`) completed the read-only integrated UI audit in `docs/design-qa.md` as `PARTIAL/BLOCKED`: deterministic web/integration gates passed, while browser, AT, exact-width and pixel gates were unavailable. The three owner findings are modal focus trap/restore in `apps/web/src/shared/site-shell.tsx`, `.quick-add` target size and reduced-motion transforms in `apps/web/src/styles.css`.
- TEST-001 Kepler (`01a09128-5ed2-7033-bc0d-3d1feebcf229`) completed the safe runtime sidecar: deterministic matrix and static Compose checks passed; isolated API liveness and SSR process/static probes were recorded; Docker-backed DB/Redis readiness, worker, browser/auth/provider and concurrency gates remain blocked or unrun. No credentials or production resources were used.
- A11Y-001 Chandrasekhar (`01a09134-c6f0-7bb3-9d82-4d754a495db9`) and A11Y-002/003 Lorentz (`01a09134-c7ab-70f2-b841-b5e38246a629`) were reviewed and integrated in `1bad6aa`. The shell focus containment/restore and CSS touch-target/reduced-motion fixes stay within their owner files; browser/AT/rendered visual validation remains unverified.
