{
  "name": "@nova/worker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "bun --watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "health": "bun src/health.ts",
    "start": "bun dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nova/config": "workspace:*",
    "@nova/db": "workspace:*"
  }
}
