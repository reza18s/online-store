# Monitoring

The API exposes `/health/live` and `/health/ready` for the first operational probes. Dashboards, alerts, and provider-failure runbooks remain release-gated work and must cover the signals listed in [`arch.md`](../../arch.md).
